replicaCount: 1
image:
  backend:
    repository: dclaw-marketing-backend
    tag: local-1779987386
    pullPolicy: Never
  frontend:
    repository: dclaw-marketing-frontend
    tag: local-1779987386
    pullPolicy: Never
service:
  backend:
    type: ClusterIP
    port: 8102
  frontend:
    type: NodePort
    port: 3015
    nodePort: 30064
ingress:
  enabled: false
secret:
  data:
    DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_marketing"
    APP_ENV: "production"
    SECRET_KEY: "dev-secret-change-me"
    OLLAMA_URL: "http://ollama:11434"
    OPENROUTER_API_KEY: ""
    NEXT_PUBLIC_API_URL: ""
    ENABLE_DEMO_MODE: "true"
resources:
  requests: { cpu: 10m, memory: 128Mi }
  limits:   { cpu: 300m, memory: 384Mi }
strategy:
  type: Recreate
