# Core framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Database / ORM
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
# psycopg2 = sync driver needed by Celery tasks (which are sync-native
# and re-write `+asyncpg` -> default sync driver in helpers.py)
psycopg2-binary>=2.9.9
alembic>=1.13.0
pgvector>=0.2.5

# Schemas
pydantic>=2.5.0
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# HTTP
httpx>=0.26.0

# Auth (A1)
fastapi-users[sqlalchemy]>=13.0.0
fastapi-users-db-sqlalchemy>=6.0.1
argon2-cffi>=23.1.0

# Background jobs (A2 — added now so we don't need a separate dep PR)
celery[redis]>=5.3.0
redis>=5.0.0

# Email (Resend)
resend>=0.7.0

# Object storage (A3)
aiobotocore>=2.11.0
boto3>=1.34.0

# Ingestion / parsers (Q2)
pypdf>=4.0.0

# LLM providers (Q3 embeddings + Phase 2 generation)
openai>=1.30.0
anthropic>=0.30.0

# Crypto (encrypted tenant secrets)
cryptography>=42.0.0

# Tests
pytest>=7.4.0
pytest-asyncio==0.24.0
aiosqlite>=0.19.0

# Observability (§6.12). sentry-sdk is optional in dev — bootstrap
# is a no-op when sentry_dsn is empty.
sentry-sdk>=2.0.0
redis>=5.0.0
