name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: write              # push branches, create commits, push tags
      pull-requests: write         # open / update / merge / close PRs
      issues: write                # comment / label / close / reopen issues
      id-token: write
      actions: write               # read CI results + trigger / cancel workflow runs
      repository-projects: write   # classic Projects (org-level Projects v2 needs app grant — see PR body)
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # This is an optional setting that allows Claude to read CI results on PRs
          additional_permissions: |
            actions: read

          # Optional: Give a custom prompt to Claude. If this is not specified, Claude will perform the instructions specified in the comment that tagged it.
          # prompt: 'Update the pull request description to include a summary of changes.'

          # Bash allowlist for end-to-end @claude tasks: PRs, issues, releases,
          # tags, workflow runs, and Projects. Admin / secrets / branch protection
          # are deliberately excluded.
          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          claude_args: >-
            --allowed-tools "Bash(gh pr *),Bash(gh issue *),Bash(gh release *),Bash(gh workflow *),Bash(gh run *),Bash(gh project *),Bash(gh label *),Bash(gh repo view),Bash(gh repo edit),Bash(gh api *),Bash(git tag *),Bash(git push *)"

